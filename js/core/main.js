/* ==========================================================================
   main.js — ponto de entrada geral do site
   Local no projeto: js/core/main.js

   Por enquanto não há lógica própria aqui: navigation.js já cuida do menu.
   Este arquivo existe para centralizar futuras inicializações que rodam
   em todas as páginas (ex.: verificar se há evento em destaque para exibir
   o #aviso-destaque, quando a integração com o Google Calendar for feita
   em integrations/calendar/).
   ========================================================================== */

document.addEventListener("DOMContentLoaded", () => {
  // Espaço reservado para inicializações futuras.
});
